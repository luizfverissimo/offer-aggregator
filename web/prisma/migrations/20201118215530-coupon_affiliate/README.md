# Migration `20201118215530-coupon_affiliate`

This migration has been generated by luizfverissimo at 11/18/2020, 6:55:30 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "AffiliateLink" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "store" TEXT NOT NULL,
    "affiliateLink" TEXT NOT NULL
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Offer" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "active" BOOLEAN NOT NULL,
    "name" TEXT NOT NULL,
    "urlImage" TEXT NOT NULL,
    "urlOffer" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "offerPrice" INTEGER NOT NULL,
    "normalPrice" INTEGER NOT NULL,
    "offerText" TEXT NOT NULL,
    "store" TEXT NOT NULL,
    "coupon" TEXT NOT NULL DEFAULT 'SEM',
    "createdAt" TEXT NOT NULL,
    "createdAtDb" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "authorId" INTEGER NOT NULL,

    FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
);
INSERT INTO "new_Offer" ("id", "active", "name", "urlImage", "urlOffer", "description", "offerPrice", "normalPrice", "offerText", "store", "createdAt", "authorId", "createdAtDb") SELECT "id", "active", "name", "urlImage", "urlOffer", "description", "offerPrice", "normalPrice", "offerText", "store", "createdAt", "authorId", "createdAtDb" FROM "Offer";
DROP TABLE "Offer";
ALTER TABLE "new_Offer" RENAME TO "Offer";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201108142925-init2..20201118215530-coupon_affiliate
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -20,8 +20,9 @@
   offerPrice Int
   normalPrice Int
   offerText String
   store String
+  coupon String @default("SEM")
   createdAt String
   createdAtDb DateTime @default(now())
   author User @relation(fields: [authorId], references: [id])
   authorId Int
@@ -40,5 +41,11 @@
   id Int @id @default(autoincrement())
   offerLink String
   createdAtDb DateTime @default(now())
   createdAt String
+}
+
+model AffiliateLink {
+  id Int @id @default(autoincrement())
+  store String
+  affiliateLink String
 }
```


