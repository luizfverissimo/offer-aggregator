# Migration `20201108142925-init2`

This migration has been generated by luizfverissimo at 11/8/2020, 11:29:25 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "OfferSuggestion" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "offerLink" TEXT NOT NULL,
    "createdAtDb" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "createdAt" TEXT NOT NULL
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Offer" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "active" BOOLEAN NOT NULL,
    "name" TEXT NOT NULL,
    "urlImage" TEXT NOT NULL,
    "urlOffer" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "offerPrice" INTEGER NOT NULL,
    "normalPrice" INTEGER NOT NULL,
    "offerText" TEXT NOT NULL,
    "store" TEXT NOT NULL,
    "createdAt" TEXT NOT NULL,
    "createdAtDb" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "authorId" INTEGER NOT NULL,

    FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
);
INSERT INTO "new_Offer" ("id", "active", "name", "urlImage", "urlOffer", "description", "offerPrice", "normalPrice", "offerText", "store", "createdAt", "authorId") SELECT "id", "active", "name", "urlImage", "urlOffer", "description", "offerPrice", "normalPrice", "offerText", "store", "createdAt", "authorId" FROM "Offer";
DROP TABLE "Offer";
ALTER TABLE "new_Offer" RENAME TO "Offer";
CREATE TABLE "new_User" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "role" TEXT NOT NULL DEFAULT 'USER',
    "password" TEXT NOT NULL,
    "createdAtDb" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);
INSERT INTO "new_User" ("id", "name", "email", "role", "password") SELECT "id", "name", "email", "role", "password" FROM "User";
DROP TABLE "User";
ALTER TABLE "new_User" RENAME TO "User";
CREATE UNIQUE INDEX "User.email_unique" ON "User"("email");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201103213439-init..20201108142925-init2
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -21,8 +21,9 @@
   normalPrice Int
   offerText String
   store String
   createdAt String
+  createdAtDb DateTime @default(now())
   author User @relation(fields: [authorId], references: [id])
   authorId Int
 }
@@ -31,5 +32,13 @@
   name String
   email String @unique
   role String @default("USER")
   password String
+  createdAtDb DateTime @default(now())
 }
+
+model OfferSuggestion {
+  id Int @id @default(autoincrement())
+  offerLink String
+  createdAtDb DateTime @default(now())
+  createdAt String
+}
```


